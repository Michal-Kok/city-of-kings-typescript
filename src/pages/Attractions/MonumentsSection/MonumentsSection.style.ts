import styled from "styled-components";

export const MonumentsWrapper = styled.div`
    width: 100%;
    padding: 200px 5%;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: ${({ theme }) => theme.colors.grey};
`;